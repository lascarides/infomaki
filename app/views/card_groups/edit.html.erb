<h1>Editing card_group</h1>

<% form_for(@card_group) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @card_group %> |
<%= link_to 'Back', card_groups_path %>
